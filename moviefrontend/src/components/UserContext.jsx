// Importing 'createContext' from the 'react' library
import { createContext } from 'react';

// Creating a new context named 'UserContext'
const UserContext = createContext();

// Exporting the 'UserContext' as the default export for use in other components
export default UserContext;
