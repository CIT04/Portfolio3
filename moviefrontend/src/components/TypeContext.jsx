// Importing 'createContext' from the 'react' library
import { createContext } from 'react';
// Creating a context named 'TypeContext' using 'createContext
const TypeContext = createContext();
// Exporting the 'TypeContext' as the default export for use in other components
export default TypeContext;